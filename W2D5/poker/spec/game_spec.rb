# require "rspec"
# require "game"

# describe Game do
    #let(:deck) {double("Deck"), length: 52}
    # subject(:game) {Game.new(deck)}

    # describe "#initialize(deck, players, pot = 0)" do
    #     it "should accept a deck and set @deck to deck" do
    #         expect(game.get_instance_variable(@deck))
    #     end
    # end
# end